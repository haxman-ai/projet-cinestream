<?php
?>

<?php require __DIR__ . '/header.phtml'; ?>

<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="css/style.css">
    <title><?= htmlspecialchars($film['title']) ?> - CineStream+</title>
</head>
<body>
<main class="container">
    <a href="index.php" class="btn-back">⬅ Retour à la vidéothèque</a>

    <section class="movie-details">
        <div class="details-layout">
            
            <div class="details-poster">
                <img src="https://image.tmdb.org/t/p/w500<?= $film['poster_path'] ?>" 
                     alt="<?= htmlspecialchars($film['title']) ?>">
            </div>

            <div class="details-info">
                <h1><?= htmlspecialchars($film['title']) ?></h1>
                
                <div class="meta">
                    <span class="date">📅 <?= $film['release_date'] ?></span>
                    <span class="rating">⭐ <?= $film['vote_average'] ?>/10</span>
                    <span class="status-badge <?= $film['isWatched'] ? 'watched' : 'to-watch' ?>">
                        <?= $film['isWatched'] ? '✅ Déjà vu' : '🕒 À regarder' ?>
                    </span>
                </div>

                <h3>Synopsis</h3>
                <p class="overview">
                    <?= !empty($film['overview']) ? htmlspecialchars($film['overview']) : "Aucun résumé disponible pour ce film." ?>
                </p>

                <div class="actions">
                    <button class="btn-edit">Modifier la fiche</button>
                </div>
            </div>

        </div>
    </section>
</main>

</body>
</html>