<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Modifier <?= htmlspecialchars($film->getTitle() ?? 'Film') ?> - CineStream+</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>

<?php require __DIR__ . '/header.phtml'; ?>

<main class="container">
    <section class="movie-details update-page">
        <div class="details-layout">
            <div class="details-poster">
                <img src="https://image.tmdb.org/t/p/w500<?= htmlspecialchars($film->getPoster_path() ?? '') ?>"
                     alt="<?= htmlspecialchars($film->getTitle() ?? 'Film') ?>">
            </div>

            <div class="details-info">
                <h1><?= htmlspecialchars($film->getTitle() ?? 'Film sans titre') ?></h1>

                <div class="meta">
                    <span>📅 <?= htmlspecialchars((string)($film->getRelease_date() ?? 'n/c')) ?></span>
                    <span>⏱ <?= htmlspecialchars((string)($film->getRuntime() ?? 'n/c')) ?> min</span>
                </div>

                <p class="overview">
                    <?= !empty($film->getOverview())
                        ? htmlspecialchars($film->getOverview())
                        : 'Aucun synopsis disponible.' ?>
                </p>

                <form action="index.php?route=update&id=<?= $film->getId() ?>" method="POST" class="update-form">
                    <input type="hidden" name="id" value="<?= $film->getId() ?>">

                    <label for="genre_id">Genre personnalisé</label>
                    <select name="genre_id" id="genre_id">
                        <option value="">-- Choisir un genre personnalisé --</option>
                        <?php foreach ($genres as $genre) : ?>
                            <option value="<?= $genre->getId() ?>" <?= $film->getGenre_id() == $genre->getId() ? 'selected' : '' ?>>
                                <?= htmlspecialchars($genre->getName()) ?>
                            </option>
                        <?php endforeach; ?>
                    </select>

                    <label for="description">Description personnelle</label>
                    <textarea id="description" name="description"><?= htmlspecialchars($film->getDescription() ?? '') ?></textarea>

                    <p>Vu / À voir</p>

                    <label>
                        <input type="radio" name="isWatched" value="1" <?= $film->getIsWatched() == 1 ? 'checked' : '' ?>>
                        Vu
                    </label>

                    <label>
                        <input type="radio" name="isWatched" value="0" <?= $film->getIsWatched() == 0 ? 'checked' : '' ?>>
                        À voir
                    </label>

                    <div class="form-actions">
                        <a href="index.php?route=show&id=<?= $film->getId() ?>" class="btn-back">
                            Retour à la fiche
                        </a>
                     <button type="submit" class="btn-save">
                  Enregistrer les modifications
                </button>
                                                    
                    </div>
                </form>
            </div>
        </div>
    </section>
</main>

</body>
</html>